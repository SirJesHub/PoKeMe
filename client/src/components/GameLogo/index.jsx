import { GAME_LOGO } from "../../utils/constants";

const GameLogo = () => {
  return (
    <img
      src={GAME_LOGO}
      style={{ width: "50%", marginTop: "10px", marginBottom: "0px" }}
    />
  );
};

export default GameLogo;
